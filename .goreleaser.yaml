version: 2

before:
  hooks:
    - go generate ./...

builds:
  - id: server
    main: ./cmd/server
    binary: gater
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-s -w -X github.com/cao7113/gater/internal/version.Version={{ .Version }} -X github.com/cao7113/gater/internal/version.Commit={{ .Commit }} -X github.com/cao7113/gater/internal/version.Date={{ .Date }}"
    env:
      - CGO_ENABLED=0
      
  - id: client
    main: ./cmd/client
    binary: gater-client
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-s -w -X github.com/cao7113/gater/internal/version.Version={{ .Version }} -X github.com/cao7113/gater/internal/version.Commit={{ .Commit }} -X github.com/cao7113/gater/internal/version.Date={{ .Date }}"
    env:
      - CGO_ENABLED=0

archives:
  - id: gater
    builds:
      - server
    formats: [tar.gz]
    name_template: "gater_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*
  - id: gater-client
    builds:
      - client
    formats: [tar.gz]
    name_template: "gater-client_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: github-native

release:
  replace_existing_artifacts: true
